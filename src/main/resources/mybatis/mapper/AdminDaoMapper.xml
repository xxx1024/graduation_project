<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.dao.AdminDao">
    <!--登录-->
    <select id="login" resultType="com.xxx.pojo.Admin">
        select id,username, password from graduation_project.admin where username=#{username} and password=#{password}
    </select>
</mapper>